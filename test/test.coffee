assert = require("assert")

describe "Create paragraph", ->
  describe "test 1", ->
    it "should create a paragraph with given text", ->
      assert.equal "2", "2"
      return
    return
  return
